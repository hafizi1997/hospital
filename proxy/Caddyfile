:80 {
    # Sanctum CSRF cookie (must reach Laravel, not the frontend)
    handle /sanctum/* {
        reverse_proxy backend:80
    }

    # Laravel API
    handle /api/* {
        reverse_proxy backend:80
    }

    # React frontend
    handle {
        reverse_proxy frontend:80
    }
}

